# もるくえ！ 運用手順

この手順書は、授業で「もるくえ！」を使う教員向けです。最初に一度だけ行う準備と、授業ごとに行う作業を分けて確認してください。

`Admin.html` は削除済みです。旧管理画面は使わず、Webモニター、設定シート、`もるくえ！` メニュー、各管理シートで運用します。

`v2.0.0` を本番反映する前に、`docs/student-fast-mode-verification.md` の手順に沿ってテスト用GASとテスト用スプレッドシートで確認してください。未検証コードを本番GASへ貼らないでください。問題があれば実機ログで原因を切り分け、必要に応じて直前の安定デプロイへ戻します。生徒APIの通常/高速切替設定は存在しません。

## 初回だけ行う準備

1. Google Driveで授業用のスプレッドシートを作成します。
2. スプレッドシートから `拡張機能` -> `Apps Script` を開きます。
3. `Code.gs`、`Student.html`、`Monitor.html` をApps Scriptに貼り付けます。
4. `appsscript.json` を表示できるようにし、リポジトリの内容で上書きします。
5. Apps Scriptのサービスから `Google Classroom API` を追加します。識別子は `Classroom` のままにします。
6. Webアプリとしてデプロイします。
7. 実行するユーザーは `自分` にします。
8. アクセスできるユーザーは学校の運用に合わせて設定します。学校内で使う場合は学校/組織内ユーザーにします。
9. デプロイ後のWebアプリURLを控えます。
10. スプレッドシートを再読み込みし、`もるくえ！` -> `⓪ 初期整備・保守` -> `⓪-1 管理シートを作成・補修` を実行します。これは既存データを保持したまま、必要な管理シートを作成し、不足している列を末尾に追加する処理です。最初の1回、アップデートで列が増えたとき、シート構成が壊れたときに使います。
11. `設定` シートの `WEB_APP_URL` に、デプロイ後に控えたWebアプリの元URLを入力します。初期設定値の編集と同期操作は、`設定` シートと onOpenメニューを併用します。
12. `もるくえ！` -> `★ 先生用URLを設定シートに出力` を実行します。設定シートに `MONITOR_URL` と `TEST_STUDENT_URL` が出力され、可能ならモニター表示用キャッシュもbest-effortで初期作成されます。
13. 通常は `MONITOR_URL` を開くとすぐWebモニターを確認できます。初期作成に失敗した場合でもURL出力は成功します。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。
14. `MONITOR_URL` と `TEST_STUDENT_URL` は先生用として扱い、生徒には共有しません。`TEST_STUDENT_URL` は先生用テストプレイURLであり、Classroomには配付しません。Webモニターのアクセス範囲はWebアプリのデプロイ設定と先生用URLの認証値で制御します。
15. 通常運用はWebモニター、設定シート、`もるくえ！` メニュー、各管理シートを使います。

## 授業前に行う準備

ここにある重い処理は、授業前に済ませることを基本にします。授業中は設定シートの `MONITOR_URL` をブックマークして、表示・履歴参照中心で使います。WebモニターURLは先生用として扱い、生徒には共有しないでください。アクセス範囲はWebアプリのデプロイ設定と先生用URLの認証値で制御します。

授業前に済ませることは、設定シートの確認、先生用URL出力、Classroom一覧取得、同期対象確認と同期対象保存、生徒名簿取得、トークン発行、DRY_RUN、URL配付、各管理シート確認です。通常フローは `① Classroom同期` -> `② 生徒URL` -> `MONITOR_URL` 確認です。管理シートの作成・補修では、既存データは保持され、不足している列だけが自動で追加されます。準備・設定・配付・保守はスプレッドシートの `もるくえ！` メニューと `設定` シートで行います。

### 1. 設定シートを確認する

設定変更は `設定` シートの `値` 列を直接編集します。各行の `説明` 列を参考に、何を入力する設定か確認してください。

初回またはアップデート後は、先に `⓪ 初期整備・保守` -> `⓪-1 管理シートを作成・補修` を実行します。これは最初の1回、アップデートで列が増えたとき、シート構成が壊れたときに使う整備用メニューです。

`POST_TEXT_TEMPLATE` はClassroom投稿本文です。必ず `{{studentUrl}}` を含めてください。主な差し込み項目は `{{氏名}}`、`{{出席番号}}`、`{{Classroom名}}`、`{{studentUrl}}` です。`DRY_RUN` は、本送信前の確認では `true`、本送信時は `false` にします。

生徒画面は常時高速ルートで動きます。通常/高速切替設定は存在しません。初回表示時のアクセス時刻即時書き込みは行わず、token情報の短時間キャッシュを使います。採点レスポンスは正誤・正答・解説の返却を優先し、解答ログは正本データとして同期保存します。Webモニターの「アクセス済み」や集計反映が少し遅れることがあります。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

`WEB_APP_URL` はWebアプリの元URLです。入力後に `もるくえ！` -> `★ 先生用URLを設定シートに出力` を実行すると、設定シートの `MONITOR_URL` と `TEST_STUDENT_URL` が更新されます。この完了時に、既存キャッシュからWebモニター表示用キャッシュをbest-effortで初期作成します。初期作成に失敗してもURL出力は成功します。授業中モニターは `MONITOR_URL` を開きます。通常はすぐWebモニターを確認できます。生徒画面の動作確認は `TEST_STUDENT_URL` を開きます。`TEST_STUDENT_URL` は先生用テストプレイURLであり、Classroomには配付しません。生徒名簿にも通常生徒として混ぜません。WebモニターURLは先生用として扱い、生徒には共有しないでください。Webモニターのアクセス範囲はWebアプリのデプロイ設定と先生用URLの認証値で制御します。Webモニターの表示・履歴参照は読み取り中心です。表示モードを切り替えられ、番号・氏名は横スクロールしても固定表示され、見出しクリックで並び替えできます。`管理スプレッドシートを開く` で管理ブックを別タブ表示でき、`表示中一覧CSV` で現在の表示・絞り込み結果だけを軽量な表示用CSVとして保存できます。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

個別対応は `トークン管理` シートで対象行を1行だけ選択して、`もるくえ！` -> `④ 個別対応` から行います。教師プレビューを開く場合は `④-1 選択行の教師プレビューURLを表示`、個別トークン再発行は `④-2 選択行のトークンを再発行`、個別URL無効化は `④-3 選択行のURLを無効化` を使います。個別再発行や個別無効化ではClassroom投稿は自動更新・自動削除されません。Classroom投稿削除も必要な場合は、従来通り `トークン管理` の `投稿削除` に `1` を入力して `③ 投稿・URL取消` を使います。

### 2. Classroom一覧を取得する

スプレッドシートの `もるくえ！` メニューから `① Classroom同期` -> `①-1 Classroom一覧を取得` を実行します。完了alertに表示される件数を確認し、必要に応じて `Classroom一覧` シートで担当Classroomが表示されることを確認します。

表示されない場合は、Googleアカウントが対象Classroomの教師になっているか、Classroom APIが有効になっているかを確認してください。

### 3. 同期対象を選ぶ

`① Classroom同期` -> `①-2 同期対象の説明を表示` を確認してから、今回使うClassroomだけを同期対象にします。不要なClassroomは空欄にします。

Classroom一覧の `同期対象` は `1` でON、空欄でOFFです。

### 4. 生徒名簿を取得する

`もるくえ！` メニューから `① Classroom同期` -> `①-3 生徒名簿を取得` を実行します。完了alertに表示されるクラス数と人数を確認し、生徒数と氏名が正しいことを確認します。

転入・転出、Classroom未参加の生徒がいる場合は、Classroom側を先に整えてから再取得してください。

### 5. トークンを発行する

`もるくえ！` メニューから `② 生徒URL` -> `②-1 トークンを発行` を実行します。生徒ごとの個別URLが `トークン管理` に作成されます。

個別URLは本人専用です。再発行した場合は、古いURLではなく新しいURLを配付してください。

### 6. DRY_RUNで配付内容を確認する

`② 生徒URL` -> `②-2 配付対象を確認` で人数を確認してから、`②-3 DRY_RUNでURL配付確認` を実行します。この段階ではClassroomへの投稿は作成されず、配付対象とログだけを確認できます。

確認する点は次のとおりです。

- 対象Classroomが正しい。
- 対象人数が想定と合っている。
- 投稿文に生徒名と個別URLが入っている。
- 配付ログに大きなエラーが出ていない。

### 7. ClassroomへURLを配付する

DRY_RUNで問題がなければ、`② 生徒URL` -> `②-4 URLをClassroomに配付` を実行します。実行前の確認ダイアログで内容を確認してください。

送信後、`配付ログ` で `SUCCESS` または配付済みの状態になっていることを確認します。失敗がある場合は、`② 生徒URL` -> `②-5 失敗分を再送` を行います。

## 授業中の流れ

### 生徒への案内

生徒には次のように伝えます。

1. Classroomのお知らせを開く。
2. 自分の名前が書かれたURLを開く。
3. 画面に自分の名前が表示されていることを確認する。
4. レベルを選んで問題を解く。
5. 数値だけを入力し、単位は入力しない。
6. 正誤と解説を確認する。
7. 直近10問正答率を見ながら練習を続ける。

回帰確認として、生徒URL初回アクセス、Lv.0〜Lv.8の問題取得（Lv.1・Lv.3は変換の選択も確認）、解答送信、次の問題が一通り動くことを確認します。

生徒画面は、2問目以降で同じレベルの次問を解答中に先読みします。次の問題へ進む体感速度は改善しますが、プリフェッチ済み問題には直前の解答結果が反映されない場合があります。適応出題は維持され、通常は次以降の問題に反映されるため、学習体験上の問題は小さい想定です。

生徒画面は常時高速ルートで動きます。初回表示時の `lastAccessedAt` 即時書き込みは行いません。生徒の初回問題表示を優先するため、Webモニターのアクセス済み表示が少し遅れることがあります。採点時の解答ログは従来通り記録されますが、採点レスポンスを優先するため `集計キャッシュ` と `問題タイプ別キャッシュ` の同期更新は行いません。生徒画面の直近10問正答率や総解答数は保存結果に合わせて更新し、キャッシュ消失時は解答ログから復元します。Webモニターは「更新」で最新の解答を反映し、問題タイプ別の記録は詳細レビューを開いて確認します。

### 教員側の確認

Webモニターの一覧を調整する場合は `表示項目を編集` を開き、チェックで列を選び、↑↓またはドラッグで順番を変更して `適用` します。番号・氏名は先頭に固定です。`標準に戻す` → `適用` で初期の列構成に戻せます。標準・フォロー・詳細・配付確認の表示モードとカスタム設定は切り替えて使えます。列設定はこのブラウザー・このスプレッドシート用に保存し、他の端末には同期しません。CSVは表示中の生徒・データ列・順番で出力します（操作ボタン列を除く）。平均解答時間の集計範囲や件数の意味は[一覧の表示項目設定](docs/monitor-column-settings.md)を参照してください。

授業中に見るべきダッシュボード項目は、配付済み人数、アクセス済み人数、解答済み人数、未実施人数、要フォロー人数、直近10問平均正答率、Lv.0〜Lv.8の解答数、配付失敗数です。

授業中は設定シートの `MONITOR_URL` を先生がブックマークして開きます。WebモニターURLは先生用として扱い、生徒には共有しないでください。アクセス範囲はWebアプリのデプロイ設定と先生用URLの認証値で制御します。Webモニターでは、配付済み人数、アクセス済み人数、解答済み人数、未実施人数、生徒別状態、Lv.0〜Lv.8の解答数・正答数、直近正答率、最終解答日時、要フォロー生徒、生徒別履歴を確認します。表示モードで標準、フォロー、詳細、配付確認を切り替えられ、番号・氏名は横スクロールしても固定表示され、見出しをクリックすると並び替えできます。必要に応じて手動更新するか、自動更新をONにして60秒ごとに確認します。この60秒自動更新も「更新」と同じ差分集計を行い、最新の解答を反映します。処理中は次の更新を重ねません。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

`★ 先生用URLを設定シートに出力` の完了時にも、既存キャッシュからこのスナップショットをbest-effortで初期作成します。更新中は進捗、失敗時は再試行の案内を表示します。`モニターキャッシュ` は自動生成の表示用キャッシュなので、先生が直接編集しないでください。正本データは `解答ログ`、`トークン管理`、`生徒名簿`、`配付ログ` です。スナップショットが壊れた場合も、正本データから再生成できます。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

授業中にモニター反映を定期更新したい場合は、`⑤ 自動更新` -> `⑤-1 集計・モニター自動更新を有効化` を使います。通常は5分程度を推奨します。`⑤ 自動更新` はフル更新系であり、生徒の採点レスポンスとは別実行で、指定間隔ごとに `集計キャッシュ`、`問題タイプ別キャッシュ`、`モニターキャッシュ` を更新します。生徒画面は常時高速ルートで動くため、採点直後の反映は遅れることがありますが、生徒の体感速度を優先する設計です。自動更新を止めたい場合は `⑤-2 集計・モニター自動更新を停止`、状態確認は `⑤-3 自動更新の状態を表示` を使います。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

生徒画面の動作確認は設定シートの `TEST_STUDENT_URL` を先生が開きます。このURLは `トークン管理` に作成されるテスト生徒用tokenです。Classroomへは配付せず、通常の生徒名簿にも混ぜません。

準備・設定・配付・保守は `もるくえ！` メニュー、設定変更は `設定` シート、ログや生データ確認は各管理シートで行います。

個別の教師プレビュー、トークン再発行、URL無効化は `④ 個別対応` を使います。`トークン管理` シートで対象生徒の行を1行だけ選択してから実行してください。Classroom投稿も消したい場合は、`投稿削除` に `1` を入力して `③ 投稿・URL取消` を使います。

要フォロー生徒の見方は、要フォローのみをONにし、未配付、配付失敗、未アクセス、未実施、正答率低い、速度上昇/正答率低下の表示を優先して確認します。まずURLが届いているか、次に問題へ入れているか、最後に正答率の低い生徒へ声かけする順が実用的です。

レベル別成績は新版の記録で確認します。Lv.0は分子量・式量、Lv.1〜4は換算、Lv.5は密度、Lv.6〜7は資料選択、Lv.8は有効数字です。「オートの進行」は保存済み解答時点の段階で、復習中や直近の手動解答も区別します。新版の更新後は「管理シートを作成・補修」→「集計キャッシュを更新」を実行してください。

集計キャッシュ更新は授業前または授業後に行います。「⑨ 集計キャッシュを更新」は全履歴から再計算する保守操作です。Webモニターの通常更新は前回からの差分を集計します。初回利用前は `★ 先生用URLを設定シートに出力` がbest-effortで初期スナップショットを作ります。重い完全更新は授業中に必要なときだけ実行します。直近の生徒操作は `解答ログ` に記録されています。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。

## 授業後に行う確認

1. 必要に応じて `⑨ 集計キャッシュを更新` を実行し、Webモニターまたは `集計キャッシュ` で生徒ごとの実施状況を確認します。
2. 必要に応じて `解答ログ` をフィルタして、レベル別や問題別の傾向を見ます。Webモニターの `表示中一覧CSV` は表示中の生徒一覧だけを保存する軽量な表示用CSVです。解答ログ全履歴CSV、問題タイプ別詳細CSV、分析CSV、苦手傾向分析、苦手傾向Classroom通知は今回の対象外です。
3. 未実施の生徒がいる場合は、Classroomのお知らせや配付ログを確認します。
4. 次回も同じClassroomで使う場合は、既存トークンをそのまま使うか、必要な生徒だけ再発行します。

## 100人規模で使うときの注意

- 授業が始まる前に、トークン発行とURL配付を済ませてください。
- 生徒画面は常時高速ルートで動きます。初回アクセス時刻や集計の即時モニター反映より、生徒画面の初回表示と採点体感を優先します。
- 本番反映前は `docs/student-fast-mode-verification.md` の実機検証手順を使い、テスト用GASで生徒画面、採点、Webモニター、集計更新、自動更新を確認してください。
- URL配付は必ずDRY_RUNで確認してから本送信してください。
- `①-1 Classroom一覧を取得`、`①-3 生徒名簿を取得`、`②-4 URLをClassroomに配付`、`⑨ 集計キャッシュを更新` などの重い処理を授業中に連打しないでください。
- 授業中に自動更新を使う場合は5分程度を推奨します。授業後や不要時は `⑤-2 集計・モニター自動更新を停止` で停止してください。
- 解答ログの書き込みには `LockService` を使っていますが、同時アクセスが多い場合はGoogle側の一時的な制限を受けることがあります。
- 生徒画面で送信に失敗した場合は、少し待ってから再送してもらってください。
- Classroom送信に失敗した生徒がいる場合は、`配付ログ` を確認し、`②-5 失敗分を再送` で失敗分だけ再送してください。
- `管理データを全削除して初期状態に戻す` はデータを削除する危険操作です。通常のシート整備は `管理シートを作成・補修` を使ってください。
- 誤配付や配付取り消しでは、対象生徒を `トークン管理` の `投稿削除` 列で指定してから `③ 投稿・URL取消` メニューを使います。

### 誤配付や配付取り消しに対応する

トークン無効化とClassroom投稿削除は別概念です。ただし、`③ 投稿・URL取消` の `③-1 投稿削除=1 のURLを無効化してClassroom投稿を削除` では、対象URLの無効化と対応するClassroom投稿削除をまとめて行います。解答ログや集計データは削除しません。

必要な場合は次の順で対応します。

1. `トークン管理` シートで、対象生徒の `投稿削除` に `1` を入力します。
2. `もるくえ！` -> `③ 投稿・URL取消` -> `③-1 投稿削除=1 のURLを無効化してClassroom投稿を削除` を実行します。
3. 対象生徒のURLは無効化され、対応するClassroom投稿が削除されます。
4. 必要ならトークンを再発行し、`② 生徒URL` -> `②-3 DRY_RUNでURL配付確認` で再確認します。
5. `② 生徒URL` -> `②-4 URLをClassroomに配付` で再配付します。

`revoked` は、空欄 = 有効、`済` = 無効化済みです。既存の 1 や TRUE、チェックボックス由来の値も無効として読み取られますが、今後システムが無効化済みとして書く値は `済` です。

`投稿削除` は、空欄 = 対象外、`1` = 次回メニュー実行時の削除対象、`済` = 投稿削除済み、`失敗` = 投稿削除に失敗、`対象なし` = 対応するClassroom投稿が見つからなかったがURL無効化は済み、という意味です。`済`、`失敗`、`対象なし` は次回の削除実行対象にはしません。再実行したい場合は、先生が `投稿削除` を再度 `1` に書き換えてください。チェックボックスではなく、文字入力で運用します。

削除結果は `トークン管理` の `revoked` / `投稿削除` / `note`、配付ログ、実行ログに残ります。行は削除せず、履歴として残します。元の配付ログは消さず、削除処理の結果として `DELETED`、`DELETE_ERROR`、`DELETE_SKIPPED` が追記されます。解答ログや集計データは削除しません。先生向けの説明や入力例では `6.02×10^23` または `6.02x10^23` の形を使い、e表記例は出しません。

## よくあるトラブルと対応

### Classroomが出ない

- 教員アカウントでログインしているか確認します。
- 対象Classroomの教師として登録されているか確認します。
- Apps Scriptのサービスに `Google Classroom API` が追加されているか確認します。
- 初回の権限確認でClassroom権限を許可したか確認します。

### 生徒名簿が取れない

- `Classroom一覧` で同期対象にチェックが入っているか確認します。
- Classroomに生徒が参加済みか確認します。
- Classroom側の生徒一覧が最新か確認します。
- もう一度 `生徒名簿取得` を実行します。

### URL配付に失敗する

- `トークン管理` に個別URLがあるか確認します。
- DRY_RUNがONのままになっていないか確認します。
- Classroomへの投稿権限を許可しているか確認します。
- `配付ログ` のエラー内容を確認し、`②-5 失敗分を再送` を実行します。

### 生徒がアクセスできない

- Webアプリのアクセス権限が生徒に許可されているか確認します。
- 生徒が学校アカウントでログインしているか確認します。
- Classroomのお知らせに配付したURLが最新か確認します。
- 別の生徒のURLを開いていないか確認します。

### token不正と表示される

- Classroomのお知らせから本人専用URLを開き直してもらいます。
- `トークン管理` で該当生徒の `revoked` が `済` になっていないか確認します。`revoked` は空欄で有効、`済` で無効化済みです。既存の 1 や TRUE も無効として読み取られます。
- トークンを再発行した場合は、新しいURLを再配付します。

### 解答が記録されない

- `解答ログ` シートがあるか確認します。
- `⓪-1 管理シートを作成・補修` が済んでいるか確認します。
- 生徒画面で送信エラーが出た場合は、再送してもらいます。
- Apps Scriptの実行ログでエラー内容を確認します。

## 技術補足

- Webアプリはデプロイした教員アカウントとして実行されます。
- Classroom一覧取得、名簿取得、URL配付にはClassroom APIを使います。
- 解答ログと集計更新はスプレッドシートに保存されます。
- 同時書き込み時の競合を避けるため、解答記録では `LockService` を使います。
- 生徒リクエスト中は問題タイプ別キャッシュを読まず、通常ランダム出題を優先します。正確な問題タイプ別反映は後処理に寄せます。
- 生徒APIは常時高速ルートです。通常/高速切替設定は存在しません。生徒アクセス、問題取得、採点時は token情報の短時間キャッシュを使い、初回表示時の `lastAccessedAt` 即時書き込みを省略します。採点時は解答ログの追記を同期で残し、集計キャッシュ更新と問題タイプ別キャッシュ更新、次問同梱はレスポンスから外します。
- 授業中の軽さを保つため、生徒APIでは管理シート全体チェック、集計キャッシュ読み書き、問題タイプ別キャッシュ読み書き、解答ログ全読みを避けます。Webモニターは `モニターキャッシュ` に保存した途中結果を使い、新しい解答を差分集計します。`表示中一覧CSV` は現在の表示・絞り込み結果だけを保存する軽量な表示用CSVで、解答ログ全履歴CSVや分析CSVは対象外です。モニターが遅い場合はApps Scriptログの `getMonitorDashboardData elapsedMs`、`cacheReadElapsedMs`、`sheetReadElapsedMs`、`jsonParseElapsedMs`、`rowCount`、`payloadBytes`、`liveFallback=false` を確認します。苦手傾向分析、苦手傾向Classroom通知、分析CSVは今後の別機能として扱います。Webモニターの更新操作は「更新」1つです。前回から増えた解答を差分集計して表示します。全件を再計算して整合性を修復する保守操作は、管理スプレッドシートの `⑨ 集計キャッシュを更新` にあります。


## 管理操作を短い手順で進める

- 一覧の下ではなく、フィルターのすぐ下に表示人数と適用中の条件を表示します。「絞り込みを解除」で検索・Classroom・状態・チェックをまとめて解除できます。表示モードと並び順はそのままです。
- 同じタブを再読込すると、表示モード・並び順・Classroom・状態・チェックを復元します。検索文字列は保存しません。同名のClassroomはクラスIDを併記して区別します。
- 詳細レビューの「前の生徒」「次の生徒」は、レビューを開いた時点の絞り込み・並び順に沿って移動します。正誤とレベルの条件を毎回選び直す必要はありません。「閉じる」またはEscapeで元の一覧へ戻れます。
- 「管理操作」から、Classroom一覧、名簿、トークン管理、配付ログ、設定、実行ログを直接開けます。表示された短い手順に沿って、リンク先の「もるくえ！」メニューを操作してください。
- 特定の生徒への対応は詳細レビューの「この生徒の管理」を使います。トークン管理や名簿の対象行を直接開き、氏名を確認してから「④ 個別対応」を実行します。該当行がない場合はその旨を表示するので、一覧や名簿を確認してください。
- リンク先は別タブで開きます。管理操作の画面を閉じると元のレビューへ戻ります。リンクの読込に失敗した場合は「管理操作を再読込」を使えます。

管理ガイドの表示だけでは、Classroomへの送信、URLの変更・停止、設定変更は行いません。先生がシート側の操作を実行した時点で変更されます。

## 現行シートの整備

アップデート後は「もるくえ！」→「⓪ 初期整備・保守」→「⓪-3 管理シートの構成を診断」で確認し、「⓪-1 管理シートを作成・補修」を実行します。現行版に不要な旧設定7項目・旧集計12列を除去します。保存済み解答・正誤・個別URLは保持します。構成は[管理シート一覧](docs/spreadsheet-schema.md)を参照してください。全初期化は通常の整備には使いません。
