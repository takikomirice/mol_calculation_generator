# もるくえ！ 運用手順

この手順書は、授業で「もるくえ！」を使う教員向けです。最初に一度だけ行う準備と、授業ごとに行う作業を分けて確認してください。

## 初回だけ行う準備

1. Google Driveで授業用のスプレッドシートを作成します。
2. スプレッドシートから `拡張機能` -> `Apps Script` を開きます。
3. `Code.gs`、`Student.html`、`Admin.html` をApps Scriptに貼り付けます。
4. `appsscript.json` を表示できるようにし、リポジトリの内容で上書きします。
5. Apps Scriptのサービスから `Google Classroom API` を追加します。識別子は `Classroom` のままにします。
6. Webアプリとしてデプロイします。
7. 実行するユーザーは `自分` にします。
8. アクセスできるユーザーは学校の運用に合わせて設定します。
9. デプロイ後のWebアプリURLを控えます。
10. スプレッドシートを再読み込みし、`もるくえ！` -> `管理シートを作成・補修` を実行します。これは既存データを残してシート構造を整える処理です。
11. `もるくえ！` -> `管理ダッシュボードを開く` を実行し、WebアプリURL、投稿文、DRY_RUN設定、レベル別のアボガドロ定数・許容誤差を確認して保存します。初期設定・同期操作は onOpenメニュー から行います。

## 授業前に行う準備

ここにある重い処理は、授業前に済ませることを基本にします。授業中は管理画面をダッシュボード確認中心で使います。

授業前に済ませることは、管理シートの作成・補修、Classroom一覧取得、同期対象保存、生徒名簿取得、トークン発行、DRY_RUN、URL配付、管理ダッシュボード確認です。

### 1. Classroom一覧を取得する

スプレッドシートの `もるくえ！` メニューから `Classroom一覧を取得` を実行します。完了alertに表示される件数を確認し、必要に応じて管理ダッシュボードで担当Classroomが表示されることを確認します。

表示されない場合は、Googleアカウントが対象Classroomの教師になっているか、Classroom APIが有効になっているかを確認してください。

### 2. 同期対象を選ぶ

今回使うClassroomだけを同期対象にします。不要なClassroomはチェックを外します。

Classroom一覧の `同期対象` は `1` でON、空欄でOFFです。

### 3. 生徒名簿を取得する

`もるくえ！` メニューから `生徒名簿を取得` を実行します。完了alertに表示されるクラス数と人数を確認し、生徒数と氏名が正しいことを確認します。

転入・転出、Classroom未参加の生徒がいる場合は、Classroom側を先に整えてから再取得してください。

### 4. トークンを発行する

`もるくえ！` メニューから `トークンを発行` を実行します。生徒ごとの個別URLが `トークン管理` に作成されます。

個別URLは本人専用です。再発行した場合は、古いURLではなく新しいURLを配付してください。

### 5. DRY_RUNで配付内容を確認する

`配付対象を確認` で人数を確認してから、`DRY_RUNでURL配付確認` を実行します。この段階ではClassroomへの投稿は作成されず、配付対象とログだけを確認できます。

確認する点は次のとおりです。

- 対象Classroomが正しい。
- 対象人数が想定と合っている。
- 投稿文に生徒名と個別URLが入っている。
- 配付ログに大きなエラーが出ていない。

### 6. ClassroomへURLを配付する

DRY_RUNで問題がなければ、`URLをClassroomに配付` を実行します。実行前の確認ダイアログで内容を確認してください。

送信後、`配付ログ` で `SUCCESS` または配付済みの状態になっていることを確認します。失敗がある場合は、失敗分再送を行います。

## 授業中の流れ

### 生徒への案内

生徒には次のように伝えます。

1. Classroomのお知らせを開く。
2. 自分の名前が書かれたURLを開く。
3. 画面に自分の名前が表示されていることを確認する。
4. レベルを選んで問題を解く。
5. 数値だけを入力し、単位は入力しない。
6. 正誤と解説を確認する。
7. 直近10問正答率を見ながら練習を続ける。

回帰確認として、生徒URL初回アクセス、初級/中級/上級の問題取得、解答送信、次の問題が一通り動くことを確認します。

### 教員側の確認

授業中に見るべきダッシュボード項目は、配付済み人数、アクセス済み人数、解答済み人数、未実施人数、要フォロー人数、直近10問平均正答率、初級/中級/上級の解答数、配付失敗数です。

管理画面はスプレッドシートの `もるくえ！` -> `管理ダッシュボードを開く` から開きます。管理画面では、配付済み人数、アクセス済み人数、解答済み人数、未実施人数、生徒別状態、初級/中級/上級の解答数・正答数、直近正答率、現在の平均速度、最終解答日時、要フォロー生徒を確認します。

要フォロー生徒の見方は、要フォローのみをONにし、未配付、配付失敗、未アクセス、未実施、正答率低い、速度上昇/正答率低下の表示を優先して確認します。まずURLが届いているか、次に問題へ入れているか、最後に正答率の低い生徒へ声かけする順が実用的です。

レベル別解答状況の見方は、初級/中級/上級の解答数で偏りを見ます。初級だけに偏っている生徒には中級へ進む声かけをし、上級で正答率が落ちている生徒には初級または中級へ戻る案内をします。

集計キャッシュ更新は授業前または授業後に行います。実行すると `集計キャッシュ` と `問題タイプ別キャッシュ` を `解答ログ` から復旧します。授業中に表示が古い場合だけ最小限に実行し、重い処理は授業中に連打しないでください。直近の生徒操作は `解答ログ` に記録されています。

## 授業後に行う確認

1. 必要に応じて `集計キャッシュを更新` を実行し、`管理ダッシュボードを開く` で生徒ごとの実施状況を確認します。
2. 必要に応じて `解答ログ` をフィルタして、レベル別や問題別の傾向を見ます。
3. 未実施の生徒がいる場合は、Classroomのお知らせや配付ログを確認します。
4. 次回も同じClassroomで使う場合は、既存トークンをそのまま使うか、必要な生徒だけ再発行します。

## 100人規模で使うときの注意

- 授業が始まる前に、トークン発行とURL配付を済ませてください。
- URL配付は必ずDRY_RUNで確認してから本送信してください。
- Classroom一覧取得、生徒名簿取得、URL配付、集計キャッシュ更新などの重い処理を授業中に連打しないでください。
- 解答ログの書き込みには `LockService` を使っていますが、同時アクセスが多い場合はGoogle側の一時的な制限を受けることがあります。
- 生徒画面で送信に失敗した場合は、少し待ってから再送してもらってください。
- Classroom送信に失敗した生徒がいる場合は、`配付ログ` を確認し、失敗分だけ再送してください。
- `管理データを全削除して初期状態に戻す` はデータを削除する危険操作です。通常のシート整備は `管理シートを作成・補修` を使ってください。

## よくあるトラブルと対応

### Classroomが出ない

- 教員アカウントでログインしているか確認します。
- 対象Classroomの教師として登録されているか確認します。
- Apps Scriptのサービスに `Google Classroom API` が追加されているか確認します。
- 初回の権限確認でClassroom権限を許可したか確認します。

### 生徒名簿が取れない

- `Classroom一覧` で同期対象にチェックが入っているか確認します。
- Classroomに生徒が参加済みか確認します。
- Classroom側の生徒一覧が最新か確認します。
- もう一度 `生徒名簿取得` を実行します。

### URL配付に失敗する

- `トークン管理` に個別URLがあるか確認します。
- DRY_RUNがONのままになっていないか確認します。
- Classroomへの投稿権限を許可しているか確認します。
- `配付ログ` のエラー内容を確認し、失敗分再送を実行します。

### 生徒がアクセスできない

- Webアプリのアクセス権限が生徒に許可されているか確認します。
- 生徒が学校アカウントでログインしているか確認します。
- Classroomのお知らせに配付したURLが最新か確認します。
- 別の生徒のURLを開いていないか確認します。

### token不正と表示される

- Classroomのお知らせから本人専用URLを開き直してもらいます。
- `トークン管理` で該当生徒のトークンが無効化されていないか確認します。
- トークンを再発行した場合は、新しいURLを再配付します。

### 解答が記録されない

- `解答ログ` シートがあるか確認します。
- `管理シートを作成・補修` が済んでいるか確認します。
- 生徒画面で送信エラーが出た場合は、再送してもらいます。
- Apps Scriptの実行ログでエラー内容を確認します。

## 技術補足

- Webアプリはデプロイした教員アカウントとして実行されます。
- Classroom一覧取得、名簿取得、URL配付にはClassroom APIを使います。
- 解答ログと集計更新はスプレッドシートに保存されます。
- 同時書き込み時の競合を避けるため、解答記録では `LockService` を使います。
- `ENABLE_ADAPTIVE_PROBLEM_SELECTION` が `true` の場合、出題時は `問題タイプ別キャッシュ` と短時間の CacheService データだけを使って problemType を調整します。`false` にすると従来通りランダム出題になります。
- 授業中の軽さを保つため、token検索、lastAccessedAt更新、解答ログ集計、集計キャッシュ更新、問題タイプ別キャッシュ更新、管理画面ログ表示は必要な行だけを読む設計です。
